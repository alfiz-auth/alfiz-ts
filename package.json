{
  "name": "alfiz-ts",
  "private": true,
  "version": "0.0.0",
  "description": "Alfiz — the authorization layer. Client, Application provider, and tooling.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/application packages/prisma packages/verify",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
