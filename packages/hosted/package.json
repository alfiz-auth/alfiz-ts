{
  "name": "@alfiz/hosted",
  "version": "0.6.0",
  "description": "The Alfiz hosted provider: the same provider contract as @alfiz/application, with its far side reached over the Alfiz Provider API — the Dashboard and Federation seam. An API connection wrapped in the contract's abstract class; fetch-only, no Node dependency.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@alfiz/core": "^0.6.0"
  },
  "devDependencies": {
    "@alfiz/application": "^0.6.0"
  }
}
